const defaultState = {}

function userReducer(state=defaultState, action) {
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state

  }
}


export default userReducer
